# True if to find parents by id
bool parent_by_pid
int32 parent_pid
# True if to find parents by name
bool parent_by_name
string parent_name
# True if performance should be recorded periodically
bool use_record_period
duration record_period
# Will always return results, set to true if results should also be logged
bool roslog_outputs
